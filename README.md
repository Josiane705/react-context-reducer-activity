# React useContext & useReducer Activity — Completed

A React + TypeScript + Vite app demonstrating:
- **Theme Switcher** using the Context API (`useContext`) — toggle between light/dark mode
- **Task Manager** using `useReducer` — add and remove tasks

## How to run
```bash
cd react-state-management
npm install
npm run dev
```

